Role: You are a Senior Full-Stack Engineer specializing in Interactive Data Visualization and AI Orchestration.Objective: Upgrade the @visualizer directory from a basic DSA tool to an elite, AI-powered "Dual-Engine Visualizer" (DSA + Code Execution Flow).Scope & Constraints:All changes must be confined to the @visualizer folder.The system must have dual-core functionality:Enhanced animations for complex structures (Trees, Graphs, Dynamic Programming tables).A step-through execution tracer that maps raw code to logical flow.Integrate AI (the "Brain").Implement a smart orchestration layer using Groq (for near-instant UI state updates/small snippets) and Gemini 1.5 Pro/Flash (for large context windows and deep architectural analysis).The AI should return structured JSON/metadata to drive the frontend animations.Key Technical Requirements:The system must handle large codebases by chunking or using Gemini’s long context to identify "hot paths" for visualization.Use the LLM to translate code logic into a visual "state tree" that the frontend can render.Ensure the two visualizers share a cohesive design language but remain modular.Task:Propose a structural plan for the @visualizer directory to support these two engines.Draft the API integration logic for Groq/Gemini to handle the "Code-to-Visual" transformation.Provide suggestions to make this world-class.Suggestions:Allow users to scrub a timeline backward and forward to see how a variable changed.Use Groq to generate the immediate step-by-step "next frame" so the UI feels lag-free, while Gemini runs in the background to generate a high-level "Map" of the entire execution.Have the AI overlay "why" something is happening (e.g., "This pointer is moving because of a recursive return") directly on the visual elements.What should be focused on first: the folder structure, or the AI orchestration logic between Groq and Gemini?